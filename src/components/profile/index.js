import React from "react";

const ProfilePage = (user) => {
    return(
        <div>
            Given user, display
            edit profile button
        </div>
    )
}

export default ProfilePage;